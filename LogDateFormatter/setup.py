from setuptools import setup, find_packages

setup(
    name="log-date-formatter",
    version="1.0.0",
    packages=find_packages(),
    license="MIT",
    description="A simple utility to format dates for logs.",
    python_requires=">=3.6",
)
